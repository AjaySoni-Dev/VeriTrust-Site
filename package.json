{
  "name": "veritrust-domain",
  "version": "1.0.0",
  "private": true,
  "description": "VeriTrust landing site and Vercel API proxy",
  "scripts": {
    "check": "node scripts/check-js.js && node scripts/check-vercel-functions.js && node scripts/check-navigation.js && node scripts/check-security.js",
    "test": "node --test \"test/unit/*.test.js\"",
    "verify:gateway": "npm run check && npm test",
    "worker": "node worker/index.js",
    "start": "vercel dev"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
