{
  "name": "veritrust-domain",
  "version": "1.0.0",
  "private": true,
  "description": "VeriTrust landing site and Vercel API proxy",
  "scripts": {
    "check": "npm run check:syntax && npm test && npm run check:security",
    "check:syntax": "node scripts/check-syntax.js",
    "check:security": "node --test tests/static-security.test.js",
    "test": "node --test tests/*.test.js",
    "test:unit": "node --test tests/security.test.js tests/api-keys.test.js tests/validators.test.js tests/billing.test.js tests/link-intelligence.test.js",
    "manifest": "node scripts/build-manifest.js",
    "audit:dependencies": "npm audit --audit-level=high",
    "start": "vercel dev",
    "start:test": "node scripts/local-test-server.js"
  },
  "dependencies": {
    "busboy": "1.6.0",
    "tldts": "7.0.16"
  },
  "engines": {
    "node": ">=20"
  }
}
