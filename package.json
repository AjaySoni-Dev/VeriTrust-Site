{
  "name": "veritrust-domain",
  "version": "1.0.0",
  "private": true,
  "description": "VERITRUST landing site and Vercel API proxy",
  "scripts": {
    "check": "node --check assets/js/config.js && node --check assets/js/detection.js && node --check assets/js/site.js && node --check api/health.js && node --check api/deepfake.js && node --check api/phishing.js && node --check lib/veritrust-api.js",
    "start": "vercel dev"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
