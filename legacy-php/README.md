This folder is legacy reference code only and is not used in the Vercel deployment.
The production API uses the Node.js serverless routes in /api.
Do not deploy this folder without separate hardening.
